<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.jiangong.mapper.UserPrivilegeMapper">

    <insert id="insert">
        insert into user_privilege (user_id,menu_id) values(#{userId},#{roleId})
    </insert>
</mapper>